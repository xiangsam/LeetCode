{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile",
            "command": "g++",
            "args": [
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}",
                "-g",
                "-Wall"
            ],
            "type": "process",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            // "problemMatcher":"$gcc" 
        }
    ]
}